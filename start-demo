#!/usr/bin/env bash
# WHERE: Hermes container — start real engine Discord demo
set -e
cd /opt/data/repos/hermes-outreach-engine
export PYTHONPATH=src
exec python -m outreach_engine.cli agent-demo start "$@"
